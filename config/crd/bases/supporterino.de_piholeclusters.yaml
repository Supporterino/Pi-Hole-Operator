---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: piholeclusters.supporterino.de
spec:
  group: supporterino.de
  names:
    kind: PiHoleCluster
    listKind: PiHoleClusterList
    plural: piholeclusters
    singular: piholecluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PiHoleCluster is the Schema for the piholeclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PiHoleCluster
            properties:
              config:
                description: ConfigSpec holds optional configuration knobs for PiHole.
                properties:
                  apiPassword:
                    description: APIPassword is the password used by the PiHole web‑server.
                    properties:
                      password:
                        description: Plain text password – only one of the two fields
                          may be set.
                        maxLength: 128
                        type: string
                      secretRef:
                        description: SecretRef points to a Secret that contains the
                          password under key "password".
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  envVars:
                    additionalProperties:
                      type: string
                    description: |-
                      EnvVars is an arbitrary map of environment variables that will be
                      injected into the PiHole container via a ConfigMap.
                    type: object
                required:
                - apiPassword
                type: object
              ingress:
                description: Ingress holds configuration for exposing the cluster
                  via an Ingress.
                properties:
                  domain:
                    description: Domain is the fully‑qualified domain that the Ingress
                      will expose.
                    minLength: 1
                    type: string
                  enabled:
                    default: false
                    description: Enabled indicates whether an Ingress resource should
                      be created.
                    type: boolean
                required:
                - enabled
                type: object
              monitoring:
                description: Monitoring holds optional monitoring configuration.
                properties:
                  exporter:
                    description: Exporter contains the exporter configuration.
                    properties:
                      enabled:
                        default: false
                        description: Enabled indicates whether a Prometheus exporter
                          should be deployed.
                        type: boolean
                    required:
                    - enabled
                    type: object
                  podMonitor:
                    description: PodMonitor contains the pod‑monitor configuration.
                    properties:
                      enabled:
                        default: false
                        description: Enabled indicates whether a Prometheus pod‑monitor
                          should be deployed.
                        type: boolean
                    required:
                    - enabled
                    type: object
                type: object
              name:
                description: Name is a human‑readable identifier for the cluster.
                minLength: 1
                type: string
              persistence:
                description: Persistence holds PVC configuration for the read‑write
                  PiHole pod.
                properties:
                  accessModes:
                    description: AccessModes lists the desired PV access modes.
                    items:
                      type: string
                    minItems: 1
                    type: array
                  size:
                    description: Size is the requested storage size, e.g. "10Gi".
                    pattern: ^[0-9]+[KMGTP]i?B$
                    type: string
                  storageClassName:
                    description: StorageClassName is the name of a StorageClass to
                      use.
                    type: string
                required:
                - size
                type: object
              replicas:
                description: Replicas is the desired number of read‑only PiHole replicas.
                format: int32
                minimum: 1
                type: integer
              serviceType:
                default: ClusterIP
                description: ServiceType controls the Kubernetes Service type.
                enum:
                - ClusterIP
                - LoadBalancer
                type: string
              sync:
                description: Sync defines optional sync behaviour for the cluster.
                properties:
                  adlists:
                    default: true
                    description: AdLists indicates whether the operator should sync
                      PiHole ad‑list files.
                    type: boolean
                  config:
                    default: true
                    description: Config indicates whether the operator should sync
                      PiHole configuration files.
                    type: boolean
                  cron:
                    description: Cron is a standard cron expression that determines
                      when the sync job runs.
                    minLength: 1
                    type: string
                required:
                - adlists
                - config
                - cron
                type: object
            required:
            - name
            - replicas
            type: object
          status:
            description: status defines the observed state of PiHoleCluster
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the PiHoleCluster resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
